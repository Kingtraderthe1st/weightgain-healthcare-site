Format all project files with Prettier.

Steps:
1. Run `npm run format` to auto-format all JS, HTML, and CSS files
2. Run `git diff --stat` to show what files were changed
3. Summarize the formatting changes made
