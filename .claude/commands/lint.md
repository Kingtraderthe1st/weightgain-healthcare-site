Run code quality checks on the project.

Steps:
1. Run `npm run lint` to check for ESLint issues
2. Run `npm run format:check` to check Prettier formatting
3. Summarize any issues found
4. If there are auto-fixable issues, ask if I should run `npm run lint:fix` and `npm run format` to fix them
