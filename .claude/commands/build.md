Run a production build of the site with Vite and report the results.

Steps:
1. Run `npm run build` to create the production build in `/dist`
2. Report any build errors or warnings
3. Show the output file sizes
4. If the build succeeds, confirm it's ready for deployment
